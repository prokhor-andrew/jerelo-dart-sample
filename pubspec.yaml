name: jerelo_sample
description: "A project that showcases 'Jerelo' usage"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  drift: ^2.30.1

  flutter_riverpod: ^3.2.0

  dio: ^5.9.0

  jerelo:
    git:
      url: https://github.com/prokhor-andrew/jerelo-dart
      ref: main

  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true